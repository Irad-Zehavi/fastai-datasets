# AUTOGENERATED! DO NOT EDIT! File to edit: ../nbs/cifar.ipynb.

# %% auto 0
__all__ = ['CIFAR10', 'CIFAR100']

# %% ../nbs/cifar.ipynb 3
from fastai.vision.all import *

import fastai_datasets.patches

# %% ../nbs/cifar.ipynb 4
def CIFAR10():
    return DataBlock(
        blocks=(ImageBlock, CategoryBlock),
        get_items=get_image_files,
        get_y=parent_label,
        splitter=GrandparentSplitter('train', 'test'),
    ).datasets(untar_data(URLs.CIFAR))

# %% ../nbs/cifar.ipynb 6
def CIFAR100():
    return DataBlock(
        blocks=(ImageBlock, CategoryBlock),
        get_items=get_image_files,
        get_y=parent_label,
        splitter=FuncSplitter(lambda p: p.parents[2].name == 'test'),
    ).datasets(untar_data(URLs.CIFAR_100))
